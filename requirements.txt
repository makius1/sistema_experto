# Dependencias del proyecto
# ---------------------------------------------------------------------------
# Las sesiones 1 a 4 usan únicamente la librería estándar de Python: los
# motores de inferencia, la lógica difusa y los operadores de Mamdani se
# implementan desde cero para comprender su funcionamiento interno.
#
# A partir de la sesión 5 se incorpora NumPy, porque la defuzzificación opera
# sobre dominios discretizados de cientos o miles de puntos y el enunciado
# pide resolverla con operaciones matriciales en lugar de ciclos.

numpy>=1.24

# El enunciado menciona scikit-fuzzy como alternativa profesional, pero
# permite explícitamente usar código de centroide propio. Se optó por lo
# segundo: escribir la fórmula obliga a entenderla, evita una dependencia
# pesada y permite manejar el caso en que no hay área que equilibrar.
#
# scikit-fuzzy>=0.5
